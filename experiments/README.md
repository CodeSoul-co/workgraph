# Experiments

Put runnable experiment code and per-experiment configuration here. Prefer one folder per experiment, with enough notes to reproduce the command, input data location, Hypha commit or branch, and output path.
