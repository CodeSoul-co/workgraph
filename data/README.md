# Data

Use this folder for tracked data documentation and small curated inputs that are safe to commit.

Ignored local data locations:

- `data/raw/`
- `data/private/`

Record where large or private datasets came from, but do not commit those files by default.
